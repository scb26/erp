# Unidex backend config for local development on Windows.
# The bundled launcher can initialize a local MySQL dev instance with root and no password.

PORT=4000
HOST=0.0.0.0
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD="Shekharb88#"
DB_NAME=unidex_customer_db
